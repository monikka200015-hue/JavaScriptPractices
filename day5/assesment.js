//Search for an element in the array and return its index.
let nums=[15,47,52,56,89,45,89];
console.log("Index is "+nums[2]);
//Calculate the average of the elements.
function average(nums) 
    let sum = 0;
    for (let i = 0; i < nums.length; i++) {
        sum += nums[i];
    }
    console.log(sum/nums.length);



    function findindexArray(array){
        let sum =0;
        for(array)
    }







    let array=(50,20,30,4,78,45,10,82,10);
    function findindexArray(array,num){
        let index="";
        for(let i=0;i<array,length;i++){
            if(array(i)==num){
                index+=(i+",")

            }

        }
        return index;

        

































































    }
average(nums);
//Write a function reverseArray() that reverses the order of the elements.
function reverseArray(num){
let max=nums[0];
    for(let i=1; i<nums.length; i++){
        if(max<nums[i]){
            max=nums[i];
            nums.push(max);
        }
    }
    console.log(nums);
}
reverseArray(nums);




function reverseArray(num){
let reverseArray=[];
for(let i=0; i<num.length+1+){
    reverseArray[i=num[num.length-1-i]];
}
return reverseArray;
}
//Write a function exists(element) that prints true if the element is present, otherwise false.
let max=nums[0];
function check(element){
    for(let i=0; i<=nums.length; i++){
        if(nums==element){
            return "true";
        }
        else if(max<nums[i]){
            max=nums[i];
            return "false"
        }
    }
}
check(5);
//Write a function checkPalindrome(word) that indicate whether the given word is palindrome or not.
function checkPalindrome(word){}
//Write a function sumOfDigi(number) that prints the sum of the given number.
function sumAllArrays(nums) {
    let sum = 0;
    for (let i = 0; i < nums.length; i++) {
        sum += nums[i];
    }
    console.log("Total: " + sum);
}
sumAllArrays(nums)